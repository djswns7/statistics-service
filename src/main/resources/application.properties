spring.application.name=Statistics-service

# MySQL ?? ??
spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name
spring.datasource.username=your_username
spring.datasource.password=your_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate ?? (DDL ??)
spring.jpa.hibernate.ddl-auto=update  # ?? ??? update, ?? ??? validate? ??
spring.jpa.show-sql=true              # ??? SQL ?? ?? ??
spring.jpa.properties.hibernate.format_sql=true  # SQL? ?? ?? ???

# Security ?? (JWT ?? ?? ?? ?? ?)
security.jwt.token.expiration-length=86400000  # ?? ?? ?? (1? = 86400000 ???)
security.jwt.token.secret-key=your-secret-key   # ?? ??? ??? ??? ?

# ?? ?? ?? (?: Gmail SMTP ??? ??? ?)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-email-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ?? ??
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG  # API ?? ? ??? ?? ??
logging.file.name=logs/spring-boot-app.log    # ?? ?? ?? ?? ??

# ?? ?? ??
server.port=8080

# ??? ??
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Swagger UI ???
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
